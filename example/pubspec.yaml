name: progressalert
description: A new Flutter project.


version: 1.0.0+1

environment:
  sdk: ">=3.1.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  progress_alert:
  get: ^4.6.6
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.5
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
dependency_overrides:
  progress_alert:
    path: ..
flutter:

  uses-material-design: true

